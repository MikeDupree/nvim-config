import something from something;

interface test {
  key1: boolean;
  key2: string;
  key3: number;
}

type test2 = string;

const CONSTANT_BABY = '';

function testFunc(arg) {

  const var1 = '';
  const var2 = '';

  if (arg) {
    return;
  }
  const err = new Error();

  console.log();
  return arg ? arg === '' : var1;
}

const s = `
test string
`;

"test"

try {
  something
}
catch () {
  everything
}
// Comment
//

// TODO Add a note
//
